package com.browserstack.webdriver.core;
import org.junit.Test;

/**
 * Created with IntelliJ IDEA.
 *
 * @author Anirudha Khanna
 */
public class AppiumDriverFactoryTest {

    @Test
    public void testName() throws Exception {
        // /* =================== Prepare ================= */
        // System.setProperty(AppiumDriverFactory.CAPABILITIES_FILE_PROP, "webdriver-config.yml");

        // /* =================== Execute ================= */
        // Map<String, String> additionalUrls = AppiumDriverFactory.getInstance().getNamedTestUrls();

        // /* =================== Verify ================= */
        // Assert.assertTrue(additionalUrls.size() > 0);
    }
}
